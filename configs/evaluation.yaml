# @package _global_
name: evaluation
seed: 0
print_config: True
ignore_warnings: True


prediction_path: ${oc.env:PREDICTIONS_PATH}/test/2025-02-06_09-31-33.881257_o/  # only needed for prediction mode
split: test
is_rome: True

error_tolerance: [ 1, 5, 10, 20, 30, 40, 50 ]

hydra:
  run:
    dir: ${oc.env:OUTPUT_DIR}/${now:%Y-%m-%d_%H-%M-%S}
  job:
    chdir: True